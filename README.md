# colorFlipper
A simple app in vanillaJs which changes background color when clicked on a button

Download the files and run the following command in the terminal to see the output in the localhost:8080

npx http-server ./ -p 8080
